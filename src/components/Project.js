const Project = (props) => {
    return (
        <>
        <h1>
            {props.title}
        </h1>
        <img alt="screenshot of project"></img>
        <p>Description of the project</p>
        </> );
}
 
export default Project;